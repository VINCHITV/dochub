# backend/app/routes/jira.py
"""
POST /jira/tickets — pre-validate and push all user stories to Jira.

This is a stub module. Full implementation is Phase 3.
The router is registered here so main.py can import it without errors.
"""

from fastapi import APIRouter

router = APIRouter()
