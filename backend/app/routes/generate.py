# backend/app/routes/generate.py
"""
POST /generate/prd  — SSE stream of PRD sections.
POST /generate/stories — SSE stream of user stories.

This is a stub module. Full implementation is Phase 3.
The router is registered here so main.py can import it without errors.
"""

from fastapi import APIRouter

router = APIRouter()
